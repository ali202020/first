add     //put the file in the staging area(it is in the local)
commit  //in the local
push    //in the server
-------
git diff //difference in locale repo 
git pull //changes in the remote server
git checkout <branchName>  //switch to branch /master
-------
git show //show us the last changes on the repository
------
we can obtain the commit id througth log
